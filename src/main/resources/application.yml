spring:
  application:
    name: test-client2


server:
  port: 8072

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl :
      defaultZone: http://localhost:8070/eureka
  instance:
    hostname: localhost